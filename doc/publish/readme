部署方式使用方式：
1.使用mvn package 打包项目

2.将打包后的jar文件放到当前目录，如包名为zconsole-0.0.1-SNAPSHOT.jar，则目录结构为：
/lib
/readme
/startup.sh
/zconsole-0.0.1-SNAPSHOT.jar

3.打开startup.sh文件修改-jar后的jar包名称为zconsole-0.0.1-SNAPSHOT.jar

4.执行sh startup.sh运行项目； 

注意：如果不是通过远程方式监控，监控本地jvm时，启动ZConsole的用户必须与被监控的jvm进程启动用户相同，否则会看不到被监控的应用



